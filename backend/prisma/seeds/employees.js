// Los empleados ahora se crean en users.js con datos reales.
// Este archivo queda como stub para compatibilidad con seed.js.
export async function seedEmployees(prisma) {
    console.log('[EMPLOYEES] Los empleados ya fueron creados en seedUsers. Nada que hacer aquí.');
    return [];
}
