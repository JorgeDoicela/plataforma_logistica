PORT=4000
DATABASE_URL="postgresql://usuario:password@localhost:5432/db_recursos_humanos?schema=public"

# =================== SEGURIDAD ===================
# Clave de encriptación para salarios
# Generar una nueva clave segura ejecutando:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY="your-32-byte-hex-key-here"

# URL del frontend (para CORS)
FRONTEND_URL="http://localhost:5173"

# =================== INFORMACIÓN ===================
# ENCRYPTION_KEY debe ser:
# - 64 caracteres hexadecimales (32 bytes)
# - Generada de forma aleatoria y segura
# - Diferente para cada ambiente (desarrollo, producción)
# - NUNCA compartir públicamente ni commitear en git
